{
  "AzureAd": {
    "Instance": "https://login.microsoftonline.com/",
    "TenantId": "",
    "ClientId": "",
    "ApplicationIdURI": "{{ Application id URI }}",
    "ValidIssuers": "https://login.microsoftonline.com/TENANT_ID/v2.0,https://sts.windows.net/TENANT_ID/"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information"
    }
  },
  "ApplicationInsights": {
    "InstrumentationKey": "{{ Telemetry instrumentation key }}",
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Information"
    }
  },
  "Storage": {
    "ConnectionString": "{{ Storage Table connection string }}"
  },
  "App": {
    "AppBaseUri": "{{ Application base path }}",
    "SecurityKey": "jwtAuthKeySecuredTest",
    "TenantId": "{{ Application tenant id }}",
    "ManifestId": "{{ Manifest id of the application }}"
  },
  "MicrosoftAppId": "{{ Bot Id }}",
  "MicrosoftAppPassword": "{{ Bot Password }}",
  "i18n": {
    "DefaultCulture": "en-US",
    "SupportedCultures": "en-US"
  },
  "SearchService": {
    "SearchServiceName": "{{ Search service name }}",
    "SearchServiceAdminApiKey": "{{ Search service admin api key }}",
    "SearchServiceQueryApiKey": "{{ Search service query api key }}"
  },
  "DiscoverTabEntityId": "{{ Entity id of the tab for personal scope manifest }}",
  "RetryPolicy": {
    "medianFirstRetryDelay": 1000,
    "retryCount": 2
  }
}